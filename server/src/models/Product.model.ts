/**
 * Product — Mongoose Model
 *
 * Fields: name, price, image, category (POPCORN|DRINK|COMBO|OTHER),
 *         description, isActive,
 *         comboItems[{ productId, quantity }] (only for COMBO),
 *         discount (% off for combo)
 */
