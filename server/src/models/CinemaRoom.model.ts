/**
 * CinemaRoom — Mongoose Model
 *
 * Seat sub-document (embedded):
 *   row (A,B,C...), col, type (NORMAL|VIP|SWEETBOX), isActive
 *
 * Fields: name (unique), rows, colsPerRow,
 *         type (STANDARD|VIP|IMAX|4DX), isActive, seats[]
 */
