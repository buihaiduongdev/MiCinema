/**
 * MongoDB Connection — Mongoose
 * 
 * Export: connectDB async function
 * Dùng: mongoose.connect(MONGODB_URI)
 * Events: connection.on('error'), connection.once('open')
 * Log: "✅ Connected to MongoDB" hoặc "❌ MongoDB connection error"
 */
