/**
 * ShowtimeList — Danh sách suất chiếu
 * 
 * Props: { movieId: string }
 * Dùng: useShowtimes(movieId) hook
 * Group by: ngày → hiển thị danh sách giờ chiếu
 * Click: navigate → /booking/:showtimeId
 */
