/**
 * BookingHistory — Lịch sử đặt vé
 *
 * Dùng: useBookingHistory() hook (useQuery + pagination)
 * Hiển thị: DataTable với các cột: mã vé, phim, suất chiếu, ghế, trạng thái
 * Filter: theo trạng thái (tất cả, đã thanh toán, đã huỷ)
 */
