/**
 * ProfileForm — Form chỉnh sửa profile
 *
 * Dùng: useForm + zodResolver(updateProfileSchema) từ @shared
 * Fields: hoTen, soDienThoai, avatar (upload)
 * Submit: useMutation PUT /api/users/profile
 *
 * Dev phụ (Tín) phụ trách
 */
