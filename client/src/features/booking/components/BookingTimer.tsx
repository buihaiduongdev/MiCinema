/**
 * BookingTimer — Đếm ngược giữ ghế
 * 
 * Dùng: useState + useEffect (setInterval)
 * State: timeLeft: number (giây, mặc định 600 = 10 phút)
 * Khi hết giờ: redirect về trang chọn ghế + thông báo
 */
