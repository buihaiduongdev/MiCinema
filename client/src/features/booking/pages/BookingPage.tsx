/**
 * BookingPage — Trang đặt vé (chọn ghế)
 *
 * Dùng: useParams() lấy :showtimeId
 * Compose: <BookingTimer /> + <SeatSelection /> + <BookingSummary />
 * Submit: tạo booking → navigate /booking/confirm/:bookingId
 */
