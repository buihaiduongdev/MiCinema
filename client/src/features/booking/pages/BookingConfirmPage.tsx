/**
 * BookingConfirmPage — Trang xác nhận + thanh toán
 *
 * Dùng: useParams() lấy :bookingId
 * Compose: <BookingSummary /> (readonly) + <PaymentForm />
 * Sau thanh toán: hiển thị <TicketResult />
 */
