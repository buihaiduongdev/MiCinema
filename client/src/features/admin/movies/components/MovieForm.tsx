/**
 * MovieForm — Form thêm/sửa phim (Admin)
 *
 * Dùng: useForm + zodResolver(createMovieSchema) từ @shared
 * Fields: tenPhim, moTa, daoDien, dienVien, theLoai (multi-select), thoiLuong, ngayKhoiChieu, poster (upload)
 * Mode: isEdit ? PUT : POST
 */
