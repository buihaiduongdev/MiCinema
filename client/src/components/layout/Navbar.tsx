/**
 * Navbar — Thanh navigation trên cùng
 * 
 * Dùng: useAuth() để hiển thị login/register hoặc avatar + menu
 * Responsive: hamburger menu trên mobile
 * Chứa: Logo, nav links (Phim, Lịch chiếu), search, auth buttons
 */
