/**
 * Loyalty Points Validation Schemas (Zod)
 * 
 * Export:
 * - loyaltyConfigSchema: z.object({ tiLeDiem, hangThanhVien: z.array({ ten, diemToiThieu }) })
 * - redeemPointsSchema: z.object({ soDiem, maDatVe? })
 * 
 * Type exports:
 * - LoyaltyConfig, RedeemPointsInput
 */
